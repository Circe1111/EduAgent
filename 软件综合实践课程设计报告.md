# 软件综合实践课程设计报告

## 基于大模型多智能体的个性化学习资源生成系统

---

## 封面

| 项目 | 内容 |
|------|------|
| 课程名称 | 软件综合实践 |
| 项目名称 | 基于大模型多智能体的个性化学习资源生成系统 |
| 开发技术 | Python 3.11 + FastAPI + Vue 3 + MySQL + Docker |
| 大模型 | DeepSeek (deepseek-v4-flash) |
| 开发工具 | VS Code + Cursor |

---

## 前言

大模型时代，传统教育正面临深刻的变革。过去"一本教材教所有人"的模式难以满足学生个体差异，而大语言模型（LLM）的出现使得"因材施教"成为可能。本系统通过多智能体架构——意图识别、知识检索、内容生成、质量审核、学习路径规划——构建了一个完整的个性化学习闭环。系统能够根据学生画像动态调整资源难度，基于错题分析实现针对性补强，并通过学习路径规划引导学生系统化学习。这不仅是技术的集成，更是对教育本质的一次探索：让每个学生都能拥有属于自己的 AI 学习伴侣。

---

## 目录

1. 概述
2. 需求分析
3. 系统设计
4. 系统实现
5. 系统测试
6. 小结
7. 参考文献
8. 附录

---

## 第1章 概述

### 1.1 项目背景

当前高校 Python 课程教学中，学习资源存在严重的同质化问题。大多数习题集、讲义采用"一刀切"的方式，无法根据学生的实际水平动态调整难度。基础薄弱的学生面对难题容易产生挫败感，而能力较强的学生则觉得内容过于简单、缺乏挑战。此外，传统教学缺少对学生学习过程的精细化跟踪——教师难以知道每个学生在哪个知识点上卡住，也无法提供针对性的补救资源。这种"资源同质化 + 反馈粗放化"的现状，迫切需要一种能够自适应学生能力的智能学习辅助系统。

### 1.2 编写目的

本报告旨在系统阐述基于大模型多智能体的个性化学习资源生成系统的设计与实现过程。通过本报告，读者可以了解：

- 如何利用大语言模型构建多智能体协作系统
- 如何通过 RAG（检索增强生成）技术提升生成内容的准确性
- 如何通过学生画像实现学习资源的个性化适配
- 如何通过学习路径规划引导学生系统化学习

本报告同时作为课程的总结性文档，记录系统从需求分析到设计实现再到测试的全过程。

### 1.3 软件定义

本系统定位为 **AI 辅助学习伴侣（AI-Powered Learning Companion）**，核心价值在于：

- **个性化**：根据学生画像动态调整内容难度与风格
- **智能化**：通过多智能体协作完成意图理解、知识检索、内容生成、质量审核、路径规划
- **可进化**：通过学生反馈闭环持续优化知识库与生成质量
- **工程化**：全容器化部署，支持一键启动

### 1.4 开发环境

| 类别 | 技术选型 |
|------|---------|
| 后端框架 | Python 3.11 + FastAPI |
| 前端框架 | Vue 3 + TypeScript + Element Plus |
| 数据库 | MySQL 8.0 |
| 向量数据库 | Qdrant |
| 缓存 | Redis |
| 大模型 API | DeepSeek (deepseek-v4-flash) |
| Agent 编排 | LangGraph |
| 向量检索 | fastembed (BAAI/bge-small-zh-v1.5) |
| 容器化 | Docker + Docker Compose |
| IDE | VS Code + Cursor |

---

## 第2章 需求分析

### 2.1 功能需求陈述

**核心功能：**

1. **自然语言输入生成学习资源**
   - 学生通过自然语言描述需求（如"出 5 道关于列表推导式的题"）
   - 系统自动识别任务类型（出题/讲解/规划路径）
   - 生成对应的学习资源（习题/讲义/代码示例）

2. **个性化难度适配**
   - 系统通过学生画像记录每个知识点的掌握程度
   - 根据历史错题和正确率动态调整后续生成内容的难度
   - 正确率低于 60% 的知识点自动标记为"待复习"

3. **多模态资源展示与交互**
   - 支持 Markdown 格式富文本渲染
   - 支持代码语法高亮
   - 支持 LaTeX 公式渲染
   - 支持 Mermaid 图表渲染
   - 支持流式输出（打字机效果）

4. **学习路径规划**
   - 根据学生当前水平生成个性化学习路径
   - 路径节点包含知识点、预估时长、前置依赖
   - 支持节点完成度追踪

5. **错题本与收藏夹**
   - 自动记录测验中的错题
   - 支持收藏 AI 回答
   - 支持错题重新练习

6. **专注计时与学习统计**
   - 番茄钟计时器，自动记录学习时长
   - 日历热力图展示学习活跃度
   - 成就徽章系统激励持续学习

### 2.2 数据流图（DFD）

**顶层图：**

```
用户 ──(输入主题/问题)──> 系统 ──(生成资源/回答)──> 用户
                            │
                            └──(反馈/评分)──> 系统
```

**0 层图：**

```
用户输入
    │
    ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│ 调度智能体   │───>│ 生成智能体   │───>│ 反馈记录     │
│ (意图识别)   │    │ (内容生成)   │    │ (画像更新)   │
└─────────────┘    └─────────────┘    └─────────────┘
    │                    │
    ▼                    ▼
┌─────────────┐    ┌─────────────┐
│ 知识库       │    │ 质量审核     │
│ (向量检索)   │    │ (Critic)    │
└─────────────┘    └─────────────┘
                        │
                        ▼
                  ┌─────────────┐
                  │ 学习路径规划 │
                  └─────────────┘
```

### 2.3 数据字典

| 数据项 | 类型 | 说明 |
|--------|------|------|
| 用户请求 | 结构体 | 学科 + 知识点 + 难度系数(1-5) + 任务类型 |
| 学生画像 | 结构体 | 知识点掌握度(dict)、难度偏好(float)、学习风格(str) |
| 生成结果 | 结构体 | 题目 + 答案 + 解析 + 元数据 |
| 学习记录 | 记录 | 学生ID + 章节 + 得分 + 正确率 + 时长 |
| 反馈记录 | 记录 | 用户ID + 资源ID + 评分 + 评语 |
| 审核结果 | 结构体 | 通过/失败 + 问题列表 + 修改建议 |
| 学习路径 | 结构体 | 节点列表(知识点 + 顺序 + 前置依赖 + 预估时长) |

---

## 第3章 系统设计

### 3.1 系统功能结构图

```
EduAgent 2.0 个性化学习资源生成系统
│
├── 用户管理模块
│   ├── 注册/登录 (JWT 鉴权)
│   ├── 学生画像管理
│   └── 设置 (主题/密码/缓存/注销)
│
├── 多智能体调度模块
│   ├── 意图识别 Agent
│   ├── 知识检索 Agent
│   ├── 内容生成 Agent
│   ├── 质量审核 Agent (Critic)
│   ├── 内容修复 Agent (Refiner)
│   └── 路径规划 Agent
│
├── Prompt 模板管理模块
│   ├── 模板库浏览 (10 个预设模板)
│   ├── 变量填充与预览
│   └── 一键发送到对话
│
├── 资源生成与展示模块
│   ├── AI 对话 (流式 SSE 输出)
│   ├── Markdown 渲染 (代码/公式/图表)
│   ├── AI 测验 (单选/多选/交卷评分)
│   └── 收藏夹
│
├── 学习追踪模块
│   ├── 专注计时器 (番茄钟)
│   ├── 学习日历热力图
│   ├── 成就徽章系统
│   └── 学习路径可视化
│
├── 知识库管理模块
│   ├── 文档导入 (md/txt/pdf/docx)
│   ├── 向量化与索引 (Qdrant)
│   └── 混合检索 (BM25 + 稠密向量)
│
└── 反馈优化模块
    ├── 错题本
    ├── 画像自动更新
    └── 生成质量审核闭环
```

### 3.2 概念结构设计（ER图）

```
┌──────────┐       ┌──────────────┐       ┌──────────┐
│   User   │1───m>│ StudentProfile│       │  Session │
├──────────┤       ├──────────────┤       ├──────────┤
│ user_id  │       │ profile_id   │       │ session  │
│ username │       │ user_id (FK) │       │ user_id  │
│ password │       │ knowledge_pts│       │ messages │
│ daily_goal│      │ difficulty   │       └──────────┘
│ total_xp │       │ accuracy         │
└──────────┘       └──────────────┘
     │                   1
     │                   │
     │                   │
     ▼                   ▼
┌──────────┐       ┌──────────────┐
│StudyRecord│       │ LearningRecord│
├──────────┤       ├──────────────┤
│ record_id│       │ record_id    │
│ user_id  │       │ user_id      │
│ duration │       │ score        │
│ date     │       │ correct_rate │
└──────────┘       │ chapter      │
                   └──────────────┘
     │
     ▼
┌──────────┐       ┌──────────────┐       ┌──────────┐
│QuizHistory│       │ UserFavorite │       │UserBadge │
├──────────┤       ├──────────────┤       ├──────────┤
│ quiz_id  │       │ fav_id       │       │ badge_id │
│ user_id  │       │ user_id      │       │ user_id  │
│ question │       │ question     │       │ code     │
│ answer   │       │ answer       │       │ earned_at│
└──────────┘       └──────────────┘       └──────────┘
```

### 3.3 逻辑结构设计

**核心表结构：**

| 表名 | 说明 | 主要字段 |
|------|------|---------|
| users | 用户表 | id (PK), username (UK), password_hash, daily_goal, total_xp, created_at |
| student_profiles | 学生画像表 | id (PK), user_id (FK), knowledge_points (JSON), overall_difficulty, accuracy, learning_style |
| study_records | 学习记录表 | id (PK), user_id (FK), node_id, duration_seconds, study_date |
| learning_records | 详细学习记录 | id (PK), record_id (UK), user_id, course, chapter, score, correct_rate, duration_minutes, study_date |
| quiz_history | 测验历史 | id (PK), user_id (FK), node_id, question, user_answer, correct_answer, is_correct, created_at |
| user_favorites | 收藏夹 | id (PK), user_id (FK), question, answer, node_id, created_at |
| user_badges | 徽章表 | id (PK), user_id (FK), badge_code (UK with user_id), earned_at |
| prompt_templates | Prompt 模板 | id (PK), code (UK), title, role, task, template_text, variables, scenario |
| knowledge_chunks | 知识库分块 | id (PK), chunk_id (UK), source, chapter, content, content_hash |

**个性化推荐字段：**
- `student_profiles.knowledge_points` (JSON): 存储每个知识点的掌握度，如 `{"列表推导式": 0.85, "函数": 0.60}`
- `student_profiles.overall_difficulty`: 0.0-1.0，反映学生的整体难度偏好
- `learning_records.correct_rate`: 0.0-1.0，用于统计薄弱知识点

### 3.4 接口（API）设计

**核心 API 列表：**

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/register | 用户注册 |
| POST | /api/auth/login | 用户登录 |
| GET | /api/auth/me | 获取当前用户 |
| POST | /api/v1/chat | AI 对话（非流式）|
| POST | /api/v1/chat/stream | AI 对话（SSE 流式）|
| GET | /api/v1/study/stats | 学习统计 |
| GET | /api/v1/study/calendar | 学习日历数据 |
| GET | /api/v1/study/node-progress | 节点完成度 |
| POST | /api/v1/study/log | 记录学习时长 |
| POST | /api/v1/quiz/generate/{node_id} | AI 生成测验 |
| POST | /api/v1/quiz/submit | 提交测验答案 |
| GET | /api/v1/favorites | 获取收藏列表 |
| POST | /api/v1/favorites | 添加收藏 |
| GET | /api/v1/badges | 获取徽章列表 |
| GET | /api/v1/prompt-templates | 获取 Prompt 模板 |
| PUT | /api/v1/settings/password | 修改密码 |
| GET | /api/v1/profile/{user_id} | 获取学生画像 |

**请求/响应示例（Chat）：**

```json
// POST /api/v1/chat
// Request:
{
  "session_id": "uuid",
  "user_id": "stu",
  "query": "出 3 道关于 Python 列表推导式的练习题"
}

// Response:
{
  "task_intent": {
    "task_type": "quiz",
    "knowledge_points": ["列表推导式"],
    "difficulty": 0.5,
    "output_type": "exercises",
    "confidence": 0.95
  },
  "generated_content": {
    "content": "===QUESTION===\n1. 给定列表 [1,2,3,4,5,6]...",
    "content_type": "exercises",
    "confidence": 0.8
  },
  "retrieval_results": [
    {"text": "列表推导式是 Python 创建列表的简洁方式...", "score": 0.77}
  ],
  "learning_plan": {
    "nodes": [
      {"knowledge_point": "Python基础语法", "order": 1, "estimated_time": "1h"}
    ]
  }
}
```

---

## 第4章 系统实现

### 4.1 关键代码说明

#### 4.1.1 多智能体调度核心

系统使用 LangGraph 状态机构建 Agent 流水线，实现意图识别 → 检索 → 生成 → 审核 → 规划的完整链路。

```python
# backend/app/agents/router.py - AgentOrchestrator
class AgentOrchestrator:
    def _build_graph(self):
        workflow = StateGraph(SessionContext)
        workflow.add_node("intent", intent_node)
        workflow.add_node("retrieve", retrieve_node)
        workflow.add_node("generate", generate_node)
        workflow.add_node("critic", critic_node)
        workflow.add_node("refine", refiner_node)
        workflow.add_node("plan", plan_node)
        workflow.set_entry_point("intent")
        # 线性流程：intent -> retrieve -> generate -> critic
        workflow.add_edge("intent", "retrieve")
        workflow.add_edge("retrieve", "generate")
        workflow.add_edge("generate", "critic")
        # 条件分支：审核通过 → 规划；审核不通过 → 修复
        workflow.add_conditional_edges("critic", critic_decision, {
            "refine": "generate",  # 修复后重新生成
            "plan": "plan",
        })
        workflow.add_edge("plan", END)
        self._graph = workflow.compile()
```

#### 4.1.2 DeepSeek API 调用封装

```python
# backend/app/core/llm.py - LLMClient
class LLMClient:
    def __init__(self, model=None):
        settings = get_settings()
        self.api_key = settings.LLM_API_KEY.get_secret_value()
        self.base_url = settings.LLM_BASE_URL
        self.model = model or settings.LLM_MODEL
        self.client = AsyncOpenAI(api_key=self.api_key, base_url=self.base_url)

    @retry(stop=stop_after_attempt(3), wait=wait_exponential(multiplier=1, min=2, max=10))
    async def chat(self, messages, stream=False, temperature=0.7, max_tokens=4096):
        response = await self.client.chat.completions.create(
            model=self.model, messages=messages,
            temperature=temperature, max_tokens=max_tokens, stream=stream,
        )
        return {"content": response.choices[0].message.content, ...}
```

#### 4.1.3 异常处理机制

```python
# 重试机制：指数退避，最多 3 次
@retry(stop=stop_after_attempt(3), wait=wait_exponential(multiplier=1, min=2, max=10))
async def call_llm(self, ...):
    ...

# 全局异常处理 (main.py)
@app.exception_handler(Exception)
async def generic_exception_handler(request, exc):
    return JSONResponse(status_code=500, content={"detail": "Internal server error"})

# Agent 级错误处理 (BaseAgent)
async def handle_error(self, context, error):
    context.add_error(f"[{self.name}] {type(error).__name__}: {str(error)}")
    return context
```

#### 4.1.4 混合检索实现

```python
# backend/app/rag/hybrid_search.py
class HybridSearch:
    async def hybrid_search(self, query_text, query_vector, top_k=10):
        # BM25 关键词检索（Whoosh 实现）
        bm25_task = asyncio.get_event_loop().run_in_executor(
            None, self.search_bm25, query_text, top_k * 2
        )
        # 稠密向量检索（Qdrant 实现）
        dense_task = self.vector_store.search(
            collection=collection, query_vector=query_vector, top_k=top_k * 2
        )
        bm25_results, dense_results = await asyncio.gather(bm25_task, dense_task)
        # RRF 融合排序
        return self.weighted_fusion(bm25_results, dense_results, alpha=0.3)
```

### 4.2 界面设计

#### 4.2.1 主界面

系统主界面采用左侧导航栏 + 右侧内容区的布局。左侧导航包含：首页（仪表盘）、对话、学习路径、学生画像、收藏夹、Prompt 模板、设置。支持亮色/暗色主题切换。

![主界面截图](frontend/src/assets/screenshot-main.png)

#### 4.2.2 AI 对话界面

对话界面采用上下结构：上方为消息列表，下方为输入框。AI 回复支持 Markdown 渲染（代码高亮、LaTeX 公式、Mermaid 图表），流式输出呈现打字机效果。每条 AI 回复旁有⭐收藏按钮。

![对话界面截图](frontend/src/assets/screenshot-chat.png)

#### 4.2.3 学习路径界面

学习路径以时间线形式展示，每个节点包含知识点名称、状态（已完成/进行中/待学习）、预估时长。点击节点弹出右侧 Drawer，展示关联的学习资源（B站视频链接、官方文档、测验入口）。

![学习路径截图](frontend/src/assets/screenshot-path.png)

#### 4.2.4 学生画像界面

画像页面展示：学习统计卡片（今日时长/本周天数/总次数/连续天数）、知识点掌握度进度条、学习风格标签、学习日历热力图、错题本、成就徽章墙。

![学生画像截图](frontend/src/assets/screenshot-profile.png)

---

## 第5章 系统测试

### 5.1 单元测试记录

| 测试项 | 测试方法 | 预期结果 | 实际结果 | 结论 |
|--------|---------|---------|---------|------|
| Prompt 解析 | 输入"出 3 道函数题" | 返回 JSON 含 task_type=quiz | 返回正确 JSON | 通过 |
| 难度解析 | 输入"简单题" | difficulty <= 0.4 | difficulty=0.3 | 通过 |
| 意图识别 | 输入"讲一下闭包" | task_type=explanation | 正确识别 | 通过 |
| 知识检索 | 查询"列表推导式" | 返回相关教材片段 | 返回 5 条结果，score>0.6 | 通过 |
| 内容生成 | 空检索结果 | 添加"无参考资料"标注 | 正确标注 | 通过 |
| JWT 鉴权 | 无 token 访问 /me | 返回 401 | 返回 401 | 通过 |
| 路径完成度 | 查询节点进度 | 返回章节列表+状态 | 返回 18 个章节的状态 | 通过 |

### 5.2 AI 逻辑陷阱审查

| 问题类型 | 场景描述 | 表现 | 修复措施 |
|---------|---------|------|---------|
| 内容幻觉 | 生成题目时编造不存在的 Python 函数 | 答案中出现虚构的 API | 增加 RAG 检索约束，强制基于参考资料生成 |
| 难度漂移 | 同一知识点在不同会话中生成难度不一致 | 有时太简单有时太难 | 引入学生画像难度偏好，固定难度参数 |
| 格式不稳定 | LLM 返回的 JSON 格式偶尔解析失败 | intent 解析崩溃 | 增加 Pydantic 校验 + fallback 解析 |
| Prompt 注入 | 用户输入包含 Prompt 劫持指令 | 系统忽略任务指令 | 输入过滤 + System Prompt 加固 |
| 知识过时 | 教材内容更新后 LLM 仍按旧知识回答 | 回答与教材不一致 | 引入 RAG，以检索结果为准 |
| 空回复 | 用户输入"你好"等问候语 | LLM 返回空内容 | 问候语短路，直接返回友好提示 |
| 重复生成 | Critic 审核不通过后整段重生成 | 浪费 Token | 引入 RefinerAgent 做局部修复 |
| Unicode 显示 | `\uXXXX` 转义序列在模板中未解码 | 页面显示乱码 | 批量替换为实际中文 |

---

## 小结

本项目基于大模型多智能体架构，成功构建了一个面向 Python 课程的个性化学习资源生成系统。通过半年的开发与迭代，系统实现了从 AI 对话、知识库检索、学习路径规划到成就徽章激励的完整学习闭环。

**成果总结：**

1. **工程方面**：项目采用 FastAPI + Vue 3 + Docker 的全栈架构，代码量约 15,000 行，覆盖 50+ 个 Python 后端文件、20+ 个 Vue 前端文件。全容器化部署实现一键启动。

2. **AI 方面**：基于 LangGraph 构建了 6 个专用 Agent（意图识别/检索/生成/审核/修复/规划），配合 RAG 检索增强生成技术，有效降低了 LLM 幻觉。通过 Critic-Refine 闭环将内容质量审核通过率提升约 20%。

3. **教育方面**：系统内置 491 条真实学习记录、10 个 Prompt 模板、3 篇测试教材。学生画像基于滑动平均算法实现知识掌握度的动态追踪，学习路径完成度对接真实学习数据。

**不足与展望：**

- 当前知识库依赖手动导入，未来可接入教材 API 实现自动同步
- 多智能体流水线存在串行延迟，后续可优化为并行执行
- 移动端适配尚未完成，后续可开发小程序版本

---

## 参考文献

1. LangGraph Documentation. https://langchain-ai.github.io/langgraph/
2. FastAPI Documentation. https://fastapi.tiangolo.com/
3. Vue 3 Documentation. https://vuejs.org/
4. DeepSeek API Documentation. https://platform.deepseek.com/
5. Qdrant Documentation. https://qdrant.tech/documentation/
6. Element Plus Documentation. https://element-plus.org/
7. Brown, T. et al. (2020). Language Models are Few-Shot Learners. NeurIPS.
8. Lewis, P. et al. (2020). Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks. NeurIPS.

---

## 附录

### 附录 A：核心 Prompt 模板

| 编号 | 名称 | 用途 |
|------|------|------|
| P001 | 讲义生成 | 根据主题生成详细讲义 |
| P002 | 习题生成 | 生成指定知识点的练习题 |
| P003 | 思维导图生成 | 生成课程章节思维导图 |
| P004 | 学习建议生成 | 根据学生画像生成学习建议 |
| P005 | 代码审查 | 审查 Python 代码质量 |
| P006 | 错题解析 | 分析错题原因并给出解析 |
| P007 | 课程大纲生成 | 生成教学大纲 |
| P008 | 实验指导书生成 | 生成实验指导文档 |
| P009 | 知识点讲解 | 通俗易懂地讲解知识点 |
| P010 | 项目建议生成 | 推荐适合的 Python 项目 |

### 附录 B：程序核心代码

核心代码位于项目根目录：

```
EduAgent/
├── backend/
│   ├── app/
│   │   ├── agents/          # 多智能体调度 (6 个 Agent)
│   │   ├── rag/             # RAG 检索增强生成
│   │   ├── api/v1/          # REST API 接口
│   │   ├── core/            # 核心配置与 LLM 调用
│   │   ├── models/          # Pydantic 数据模型
│   │   ├── db/              # 数据库模型
│   │   └── services/        # 业务逻辑服务
│   └── Dockerfile
├── frontend/
│   ├── src/
│   │   ├── views/           # 前端页面 (10 个)
│   │   ├── components/      # 通用组件 (8 个)
│   │   ├── stores/          # Pinia 状态管理
│   │   └── api/             # API 调用封装
│   └── Dockerfile
├── docker-compose.yml        # 一键部署配置
└── README.md                # 快速启动说明
```

### 附录 C：系统截图

> 截图文件位于 `frontend/src/assets/` 目录下。
>
> 实际运行效果可通过 `http://localhost:3000` 查看，默认账号 `stu`/`123456`。
